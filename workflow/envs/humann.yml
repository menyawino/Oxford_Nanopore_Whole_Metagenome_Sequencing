name: humann_env

channels:
  - bioconda
  - conda-forge
  - defaults

dependencies:
  - python=3.8
  - humann=3.0.0
  - diamond=2.0.15
  - metaphlan=3.0
  - numpy
  - scipy
  - pandas
  - biom-format